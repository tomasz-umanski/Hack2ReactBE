spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    password: admin
    username: admin
    url: jdbc:h2:mem:my-flowers
  jpa:
    hibernate:
      ddl-auto: create